import {Assets} from '~last/shared/assets'
import {InPageNav} from '~last/theme/react';

# Last: an exploration game
🕑 &nbsp; Last updated 2ss days ago
<hr/>
<aside>TABLE_OF_CONTENTS</aside>
<InPageNav/>
<br/>
## In page table of contentsdd
- Using `@joe307bad/rehype-toc`, we can debug an implementation of replacing a placeholder text (eg. TABLE_OF_CONTENTS) to a TOC
- The `@joe307bad/rehype-toc` will only run if there is no `.next` cache in `dist/apps/web/docs/` (so this may have to be deleted in order to debug)
## Navigation brainstorm
- Documentation - set up development environment and building blocks of application
- Architecture - explanation of each project in the monorepo
  - List of services that leads to a status page of services and past issues
- [is this link clickable](https://aws.com)
## Project Overview
### Project Purpose
- Building and facilitating an **unpredictable digital universe**
- Maintaining a scalable and collaborative code base
### Latest development goal
* [ ] Build out documentation theme
  - Ensure layout parity with [Bitbucket markdown syntax](https://confluence.atlassian.com/bitbucketserver/markdown-syntax-guide-776639995.html)
  - Code highlighting
  - https://github.com/wokwi/good-arduino-code/commit/839400af07a67420dcb2f75b8226ef482f4368cb
  - https://github.com/vercel/next.js/discussions/11419
  - might be able to use this for loading external mdx files https://github.com/vercel/next.js/discussions/12149#discussioncomment-314338
* [ ] Brainstorm real time strategy game integration
<br/>
## Development
### Steps for running local environment
1. Clone repo
2. Run `docker-compose up -d`
### Opening a PR
Some good advice from [Atlassian](https://www.atlassian.com/blog/git/written-unwritten-guide-pull-requests):
>Making smaller pull requests is the #1 way to speed up your review time. Because they are so small, it’s easier for the reviewers to understand the context and reason with the logic.
### Common commands using the Last CLI
Here is an example of claiming a planet:
```javascript
var oldUnload = window.onbeforeunload;
window.onbeforeunload = function() {
    saveCoverage();
    if (oldUnload) {
        return oldUnload.apply(this, arguments);
    }
};
```
<br/>
## Assets
<img height={30} width={30} src={Assets.logo}/>
<br/>
## Tables
Here is an example of a table:
| Day     | Meal    | Price |
| --------|---------|-------|
| Monday  | pasta   | $6    |
| Tuesday | chicken | $8    |
